import React from 'react'
import { mount, shallow } from 'enzyme';
import { expect } from 'chai';
import Hint from '../../hint'

describe('Test Hint component', () => {
  it('Test default props', function() {
    const result = mount(<Hint></Hint>);

  });
});
